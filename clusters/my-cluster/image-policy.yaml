apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: todo-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: todo-repo
  policy:
    alphabetical:
      order: asc