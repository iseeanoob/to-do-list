apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: todo-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: todo-repo
  policy:
    semver:
      range: '^1.0.0'