name: Test and Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: sett opp Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'
      - run: npm install
      - run: npm test